import 'package:flutter/material.dart';

class DriverTripScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('Driver Trip Screen'),
      ),
    );
  }
}
